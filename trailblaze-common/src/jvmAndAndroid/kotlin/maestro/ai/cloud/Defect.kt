package maestro.ai.cloud

/**
 * Stub class to satisfy Maestro runtime requirements without including the full `maestro-ai` artifact.
 * Maestro's AI features are tied to their proprietary backend.
 *
 * IMPORTANT: DO NOT DELETE THIS FILE
 * - Required for Maestro to function properly at runtime on Android
 * - Without this stub, Maestro will fail with ClassNotFoundException
 * - This is a minimal data class stub, not actual functionality
 * - Required for both JVM (host mode) and Android (on-device) builds
 */
@Suppress("UNUSED")
@Deprecated(
  "This is a runtime stub to satisfy Maestro dependencies. Do not use.",
  level = DeprecationLevel.HIDDEN
)
class Defect
