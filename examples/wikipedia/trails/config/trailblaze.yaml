# Workspace anchor for the Wikipedia example. The example's authored unit lives at
# trailmaps/wikipedia/trailmap.yaml; the Trailblaze daemon picks the target up by name when
# this directory is set as TRAILBLAZE_CONFIG_DIR.
targets:
  - wikipedia
