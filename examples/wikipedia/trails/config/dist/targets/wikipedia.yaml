# GENERATED BY trailblaze check. DO NOT EDIT.
# Source trailmap: trailmaps/wikipedia/trailmap.yaml
# Regenerate with: trailblaze check

id: "wikipedia"
display_name: "Wikipedia (en)"
platforms:
  "web":
    tool_sets:
    - "web_core"
    - "web_verification"
    - "memory"
    drivers:
    - "playwright-native"
    - "playwright-electron"
system_prompt: "# Wikipedia target system prompt\n\n**You are controlling a Playwright-driven\
  \ web browser on\n{{device_description}}, driving live `en.wikipedia.org`.**\n\n\
  You'll be given the current screen state — a text representation of the\nDOM hierarchy\
  \ plus a screenshot of the rendered viewport. Screenshots may\nbe marked with colored\
  \ boxes containing `ref` ids for interactive elements.\n\n## UI interaction hints\n\
  \n- Wikipedia's main page regenerates daily. Section *wrappers* (`#mp-tfa`,\n  `#mp-itn`,\
  \ `#mp-dyk`, `#mp-otd`) are stable; section *headings* drift\n  copy day-to-day.\
  \ Prefer the wrapper anchors over visible heading text.\n- The header search box\
  \ has `name=\"search\"` on its input element. Both\n  Vector-2022 and the legacy\
  \ skin keep this attribute, so a CSS selector\n  on `input[name='search']` is more\
  \ reliable than ARIA-role matching.\n- Article first heading is `#firstHeading`.\
  \ Body wrapper is\n  `#mw-content-text`. Use these structural anchors when verifying\n\
  \  article-shaped pages.\n- A fundraising banner may or may not be present on any\
  \ given visit; do\n  **not** assume its presence. Use the `wikipedia_web_dismissBannerIfPresent`\n\
  \  tool, which no-ops cleanly when nothing is shown.\n\n## Special tools — always\
  \ prefer these for the listed tasks\n\nThis target ships scripted helpers that encode\
  \ Wikipedia-specific\nselector knowledge and conditional UI handling. Pick them\
  \ over generic\n`web_*` builtins for the following task families:\n\n- **\"Open\
  \ Wikipedia\" / \"go to Wikipedia main page\" / \"start from\n  Wikipedia\"** →\
  \ `wikipedia_web_openMainPage`.\n  Loads `Main_Page`, verifies render, dismisses\
  \ any fundraising banner.\n- **\"Open the X article\" / \"read the Y page on Wikipedia\"\
  \ / \"navigate\n  to the Z Wikipedia article\"** → `wikipedia_web_openArticle` with\
  \ the\n  article title (`title: \"Albert Einstein\"`).\n  Handles URL encoding +\
  \ verifies `#firstHeading`.\n- **\"Search Wikipedia for X\" / \"look up Y on Wikipedia\"\
  \ / \"find articles\n  about Z\"** → `wikipedia_web_searchAndOpenFirstResult` with\
  \ the query.\n  Types into header search, submits, asserts the destination article.\n\
  - **\"Open a random Wikipedia article\" / \"jump to a random page\" /\n  \"click\
  \ Random article\"** → `wikipedia_web_openRandomArticle`.\n  Asserts a new article-shaped\
  \ page loaded (#firstHeading visible).\n- **\"Verify the [Did you know / In the\
  \ news / Today's featured article /\n  On this day] section is visible\"** → `wikipedia_web_openMainPageSection`\n\
  \  with the short code (`section: dyk` / `itn` / `tfa` / `otd`).\n  Anchors on `#mp-<id>`\
  \ so daily copy drift can't break the test.\n- **\"Switch to Spanish / view in another\
  \ language / read the German\n  version\"** → `wikipedia_web_switchArticleLanguage`\
  \ with the title +\n  `languageCode` (`es` / `fr` / `de` / …).\n  Jumps to the matching\
  \ subdomain directly, bypassing the per-skin\n  language picker.\n- **\"Verify the\
  \ article is well-formed\" / \"confirm an article rendered\"\n  / \"make sure the\
  \ page has body content and References\"** →\n  `wikipedia_web_verifyArticleStructure`.\n\
  \  Asserts heading + body + (optional) References scroll.\n- **\"Search for X and\
  \ verify the article\" / \"look up Y and confirm it\n  rendered correctly\"** →\
  \ `wikipedia_web_searchAndVerify` with the query\n  (and `requireReferences: true`\
  \ if the task explicitly asks for a\n  References-section check). Composes `searchAndOpenFirstResult`\
  \ +\n  `verifyArticleStructure` into a single call so the agent doesn't have\n \
  \ to chain them itself.\n- **\"Close the banner\" / \"dismiss the donate prompt\"\
  \ / \"clear the\n  fundraiser popup\"** → `wikipedia_web_dismissBannerIfPresent`.\n\
  \  No-op when no banner is showing; safe to call unconditionally.\n\nFor everything\
  \ else — clicking a link in an article, scrolling, filling\na non-search form field,\
  \ asserting visible text by snippet — use the\nbuilt-in `web_*` tools (`web_click`,\
  \ `web_scroll`, `web_type`,\n`web_verifyTextVisible`, etc.). The scripted tools\
  \ above are the\n**only** ones you should reach for when the task matches one of\
  \ those\npatterns; they encode behavior the LLM would otherwise have to\nre-derive\
  \ from snapshot + heuristics on every run.\n\n## Pass scripted-tool arguments explicitly\n\
  \n`inputSchema` fields are marked `required: false` where the tool has a\nuseful\
  \ default, but the MCP runtime currently enforces a present-key\ncontract on every\
  \ property. Pass every argument explicitly when calling\na scripted tool — e.g.\
  \ include `dismissBanner: true`, `ensureOnMainPage:\ntrue`, `requireReferences:\
  \ true` on the defaults rather than omitting\nthem.\n\n## Verification preferences\n\
  \n- When verifying a Wikipedia article, anchor on `#firstHeading` (via\n  `web_verifyElementVisible`)\
  \ **before** any text-content checks. The\n  text checks can otherwise false-positive\
  \ on search-results snippets\n  or sidebar entries that happen to repeat the title.\n\
  - When verifying a main-page section, anchor on `#mp-<id>` (via the\n  scripted\
  \ `wikipedia_web_openMainPageSection` tool). The heading text\n  is unreliable across\
  \ daily content refreshes.\n"
tools:
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_openMainPage.ts"
  name: "wikipedia_web_openMainPage"
  description: "Open Wikipedia's Main_Page. Use this when the task asks to navigate\
    \ to\nWikipedia, open Wikipedia, go to the Wikipedia home page, or start from\n\
    the main Wikipedia page. Loads en.wikipedia.org/wiki/Main_Page and waits\nfor\
    \ it to render. Optionally dismisses any fundraising banner that's\ncurrently\
    \ showing so subsequent steps don't have to reason about it."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "dismissBanner":
        "type": "boolean"
        "description": "When true (default), tries to close any visible fundraising\
          \ banner."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_openArticle.ts"
  name: "wikipedia_web_openArticle"
  description: "Open a specific Wikipedia article by its title. Use this whenever\
    \ the task\nis to read, open, navigate to, or view a particular Wikipedia article\
    \ —\ne.g. \"open the Albert Einstein article\", \"go to the Python (programming\n\
    language) page\", \"navigate to the Shakespeare article\". Spaces in the\ntitle\
    \ are normalized to underscores (Wikipedia's URL convention) so you\ncan pass\
    \ the title verbatim. Verifies the article rendered by checking\nthe #firstHeading\
    \ element is visible."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "title":
        "type": "string"
        "description": "Article title to open (spaces → underscores in URL)."
      "expectedHeading":
        "type": "string"
        "description": "Visible heading text to assert. Defaults to `title`."
      "dismissBanner":
        "type": "boolean"
        "description": "When true (default), tries to close any visible fundraising\
          \ banner."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_dismissBannerIfPresent.ts"
  name: "wikipedia_web_dismissBannerIfPresent"
  description: "Dismiss any visible Wikipedia fundraising / fundraiser banner. Use\
    \ this\nwhenever the task is to close a banner, dismiss a popup, or clear out\n\
    Wikipedia's donate prompt. If no banner is currently visible the tool is\na no-op\
    \ that returns successfully, so it's safe to call unconditionally\nat the start\
    \ of a flow. Probes with a sub-second visibility check before\nattempting the\
    \ click so the no-banner path is near-instant."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "additionalProperties": false
    "description": "Input args for `wikipedia_web_dismissBannerIfPresent` — empty,\
      \ the tool takes no parameters."
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_searchAndOpenFirstResult.ts"
  name: "wikipedia_web_searchAndOpenFirstResult"
  description: "Search Wikipedia from the header search box. Use this whenever the\
    \ task\nis to search Wikipedia for something — e.g. \"search for Albert Einstein\"\
    ,\n\"look up Python on Wikipedia\", \"find articles about Mount Everest\". Types\n\
    the query into the header search input, submits the form, and verifies\nthe resulting\
    \ article's first heading. Pass `openFirstResult=false` to\nonly type the query\
    \ without submitting (useful for autocomplete-suggestion\ntests)."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "query":
        "type": "string"
        "description": "Query to type into the search box."
      "expectedHeading":
        "type": "string"
        "description": "Heading text to assert on the opened article. Defaults to\
          \ `query`."
      "openFirstResult":
        "type": "boolean"
        "description": "Submit the search form (default true)."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_openRandomArticle.ts"
  name: "wikipedia_web_openRandomArticle"
  description: "Open a random Wikipedia article via the sidebar's \"Random article\"\
    \ link.\nUse this whenever the task is to navigate to a random article, click\n\
    Random article, jump to a random page, or surf to an unpredictable\narticle. Verifies\
    \ that a new article page rendered (its #firstHeading\nis visible) — the resulting\
    \ title is non-deterministic so don't try to\nassert specific heading text afterwards."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "ensureOnWikipedia":
        "type": "boolean"
        "description": "Open the main page first if not on Wikipedia."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_verifyArticleStructure.ts"
  name: "wikipedia_web_verifyArticleStructure"
  description: "Verify the currently loaded page is a well-formed Wikipedia article.\
    \ Use\nthis whenever the task is to verify an article's structure or layout —\n\
    confirm a Wikipedia article rendered, check that an article has body\ncontent,\
    \ confirm it has a References section, or sanity-check that\nnavigation actually\
    \ landed on an article (not a search-results or\ndisambiguation page). Asserts\
    \ #firstHeading is visible, the body wrapper\nis visible, and optionally scrolls\
    \ to confirm the \"References\" section\nis present."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "expectedHeading":
        "type": "string"
        "description": "Visible text to assert in #firstHeading."
      "requireReferences":
        "type": "boolean"
        "description": "Scroll-to-bottom + assert References heading (default true)."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_openMainPageSection.ts"
  name: "wikipedia_web_openMainPageSection"
  description: "Verify one of Wikipedia's four Main_Page sections is present and visible.\n\
    Use this whenever the task is to verify a main-page section: \"Did you\nknow\"\
    , \"In the news\", \"On this day\", or \"From today's featured article\"\n(also\
    \ known as DYK, ITN, OTD, TFA). Pass the section short code to pick\nwhich one.\
    \ The tool anchors verification on the section's stable wrapper\nelement id (`#mp-dyk`\
    \ etc.) — which survives Wikipedia's day-to-day copy\ndrift — rather than the\
    \ visible heading text (which rotates daily)."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "section":
        "type": "string"
        "description": "Section short code: tfa | itn | dyk | otd."
      "ensureOnMainPage":
        "type": "boolean"
        "description": "Open Main_Page first if not there (default true)."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_switchArticleLanguage.ts"
  name: "wikipedia_web_switchArticleLanguage"
  description: "Open the same Wikipedia article in a different language. Use this\n\
    whenever the task is to switch language, view in another language, or\nread the\
    \ Spanish/French/German/Japanese/etc. version of a Wikipedia\narticle. Routes\
    \ directly to the matching language subdomain\n(`es.wikipedia.org`, `fr.wikipedia.org`,\
    \ …) rather than driving through\nthe in-page language picker (which varies a\
    \ lot across skins). Verifies\nthe destination article's first heading is visible."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "title":
        "type": "string"
        "description": "English article title."
      "languageCode":
        "type": "string"
        "description": "Wikipedia language code (`es`, `fr`, `zh-min-nan`, …)."
      "expectedHeading":
        "type": "string"
        "description": "Visible heading text in the localized article."
    "additionalProperties": false
    "definitions": {}
- script: "/Users/samedwards/Development/trailblaze/examples/wikipedia/trails/config/trailmaps/wikipedia/tools/wikipedia_web_searchAndVerify.ts"
  name: "wikipedia_web_searchAndVerify"
  description: "Search Wikipedia for a topic AND verify the resulting article is\n\
    well-formed — heading + body, optionally References — in a single tool\ncall.\
    \ Use this whenever the task combines \"search for X\" with \"verify\nthe article\
    \ rendered correctly\", e.g. \"search Wikipedia for Python and\nconfirm the article\
    \ loaded with a body and References section\". Composes\nthe existing `wikipedia_web_searchAndOpenFirstResult`\
    \ and\n`wikipedia_web_verifyArticleStructure` tools so callers get both behaviors\n\
    with one round-trip."
  _meta:
    "trailblaze/supportedPlatforms":
    - "web"
    "trailblaze/requiresContext": true
  inputSchema:
    "$schema": "http://json-schema.org/draft-07/schema#"
    "type": "object"
    "properties":
      "query":
        "type": "string"
        "description": "Query to type into the header search box."
      "expectedHeading":
        "type": "string"
        "description": "Heading text to assert on the opened article. Defaults to\
          \ `query`, which works for exact-match articles like \"Python (programming\
          \ language)\"."
      "requireReferences":
        "type": "boolean"
        "description": "Whether to also scroll-and-assert the References section is\
          \ present. Defaults to false because not every article has one."
    "additionalProperties": false
    "definitions": {}
