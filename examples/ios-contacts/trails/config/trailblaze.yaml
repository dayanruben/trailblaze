# Workspace anchor for the iOS Contacts example. The example's authored unit lives at
# packs/contacts/pack.yaml; the Trailblaze daemon picks the target up by name when
# this directory is set as TRAILBLAZE_CONFIG_DIR.
targets:
  - contacts
