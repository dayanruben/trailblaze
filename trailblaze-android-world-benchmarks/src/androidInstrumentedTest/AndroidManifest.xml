<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

  <!-- Koog 1.0.0 raised minSdkVersion to 35 on its Android variants. This androidTest APK
       only uses JVM-portable Koog surface (LLM client constructors, message types,
       HttpClient setup); override the library minSdk so it can still target API 26.

       NOTE: This file lives at `src/androidInstrumentedTest/AndroidManifest.xml`, NOT
       `src/androidTest/`. In a KMP module with an android target, AGP looks for the
       androidTest manifest under the `androidInstrumentedTest` source set; placing it
       at `src/androidTest/` causes AGP to silently ignore the file (no merge, no
       warning), and the override below is never applied. `processDebugAndroidTestManifest`
       then fails with "minSdkVersion 26 cannot be smaller than version 35 declared in
       library [ai.koog:prompt-executor-anthropic-client-android]". Sister non-KMP modules
       (e.g. trailblaze-android) use `src/androidTest/AndroidManifest.xml` and are
       unaffected because AGP's stock `androidTest` source set is not renamed there. -->
  <uses-sdk tools:overrideLibrary="
      ai.koog.agents.core,
      ai.koog.koog.agents,
      ai.koog.agents.features.memory,
      ai.koog.agents.features.opentelemetry,
      ai.koog.agents.features.snapshot,
      ai.koog.agents.features.tokenizer,
      ai.koog.agents.mcp.metadata,
      ai.koog.embeddings.llm,
      ai.koog.prompt.cache.files,
      ai.koog.prompt.cache.model,
      ai.koog.prompt.executor.cached,
      ai.koog.prompt.xml,
      ai.koog.agents.ext,
      ai.koog.agents.features.event.handler,
      ai.koog.agents.features.trace,
      ai.koog.agents.tools,
      ai.koog.agents.utils,
      ai.koog.embeddings.base,
      ai.koog.http.client.core,
      ai.koog.http.client.ktor,
      ai.koog.prompt.executor.anthropic.client,
      ai.koog.prompt.executor.clients,
      ai.koog.prompt.executor.google.client,
      ai.koog.prompt.executor.model,
      ai.koog.prompt.executor.ollama.client,
      ai.koog.prompt.executor.openai.client,
      ai.koog.prompt.executor.openai.client.base,
      ai.koog.prompt.executor.openrouter.client,
      ai.koog.prompt.llm,
      ai.koog.prompt.markdown,
      ai.koog.prompt.model,
      ai.koog.prompt.processor,
      ai.koog.prompt.structure,
      ai.koog.prompt.tokenizer,
      ai.koog.rag.base,
      ai.koog.serialization.core,
      ai.koog.utils
  " />
</manifest>
