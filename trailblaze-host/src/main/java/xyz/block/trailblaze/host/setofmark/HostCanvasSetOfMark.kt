package xyz.block.trailblaze.host.setofmark

/**
 * Re-export from [xyz.block.trailblaze.setofmark.HostCanvasSetOfMark] for backward compatibility.
 * The canonical implementation now lives in trailblaze-common so that all JVM modules
 * (playwright, compose, host) can use it for set-of-mark annotations.
 */
typealias HostCanvasSetOfMark = xyz.block.trailblaze.setofmark.HostCanvasSetOfMark
