id: calendar_android_settingsToCalendar
description: From Settings root, tap the 'Local' calendar row to open its per-calendar settings.
shortcut:
  from: calendar/android/settings_root
  to: calendar/android/settings_calendar
parameters: []
tools:
  - tapOnElementBySelector:
      reason: ""
      nodeSelector:
        androidAccessibility:
          textRegex: "^Local$"
