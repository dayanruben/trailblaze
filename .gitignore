# Android Studio
**/.idea/*
!/.idea/vcs.xml
!/.idea/copyright/
*.iml

# Gradle
.gradle
gradlew.bat
build/
local.properties

# Kotlin
.kotlin

# Ignore pre-compiled APKs bundled as resources (built during desktop app build)
/trailblaze-desktop/src/main/resources/apks/

# Gradle Profiler
profile-out*
gradle-user-home

# Steve Jobs
.DS_Store

# SQLDelight
*.db-shm
*.db-wal

# For local customization.
local.settings.gradle

# Java heap dumps generated by "HeapDumpOnOutOfMemoryError" gradle argument.
*.hprof

# Goose hints file
.goosehints

# Github Pages mkdocs site
/site

# FOSSA
/.fossa

# Trailblaze Logs
/logs/
/trailblaze-logs/
/kotlin-js-store/yarn.lock
/.idea/vcs.xml

# Generated Kotlin JS Config Files
/kotlin-js-store/

# Node
node_modules/

# Trailblaze pack bundle — generated per-pack `.d.ts` files emitted by the
# `trailblaze.bundle` Gradle plugin (and, eventually, `trailblaze bundle` on the CLI).
# Derived from the pack manifest, regenerated on build, not source-of-truth. Anchored
# to the per-pack tools dir so an unrelated future `.trailblaze/` directory elsewhere
# in the tree (e.g., a config dir) isn't silently ignored.
**/trailblaze-config/tools/.trailblaze/
# Same generator, workspace-anchor layout (`<workspace>/trails/config/tools/.trailblaze/`).
# The `trailblaze-config/` pattern above only matches the classpath-resource path; the
# workspace path lives under `trails/config/` and needs its own anchor.
/trails/config/tools/.trailblaze/

# Daemon-emitted compiled-target output (`trailblaze compile` writes per-target YAML
# bundles here from the workspace's `packs/` definitions). Generated and lazy-rebundled
# at daemon init, never edited by hand.
/trails/config/dist/

# Daemon local data — settings JSON, cache, in-progress logs. Created in the working
# tree when the daemon's `appDataDirectory` points at the repo (developer convenience
# for "run from source" setups), kept out of version control.
/.trailblaze/

# Python
__pycache__/
*.pyc
/sdks/python/.venv/

package-lock.json
bun.lock
bun.lockb